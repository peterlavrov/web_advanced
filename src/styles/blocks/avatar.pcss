.avatar {
  width: 200px;
  height: 200px;
  overflow: hidden;
  border-radius: 50%;
  box-shadow: 0px 20px 40px $text-color;

  &__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
}